# Copy this file to .env and fill in the values.

# Where the app stores its relational data. SQLite is fine for the demo;
# switch to a Postgres URL (e.g. Neon) for production.
DATABASE_URL="sqlite:///./flow_fund.db"

# Used for embeddings + the chat assistant's LLM calls.
OPENAI_API_KEY=""
